info:
  name: Gets status info
  type: http
  seq: 1
  tags:
    - 1_API_Pubbliche

http:
  method: GET
  url: "{{baseUrl}}/public/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/public/status"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: Gets status info
