info:
  name: Api restricted to user role USER
  type: http
  seq: 1
  tags:
    - 2_User_APIs

http:
  method: GET
  url: "{{baseUrl}}/auth/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/user"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""

docs: This API say hello to the user
