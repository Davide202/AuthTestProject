info:
  name: Api restricted to user role ADMIN
  type: http
  seq: 1
  tags:
    - 3_Admin_APIs

http:
  method: GET
  url: "{{baseUrl}}/auth/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/admin"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""

docs: Empty description
