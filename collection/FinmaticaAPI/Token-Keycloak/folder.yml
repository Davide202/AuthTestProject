info:
  name: Token-Keycloak
  type: folder
  seq: 1

request:
  variables:
    - name: keycloak_base_url
      value: http://localhost:8081
    - name: keycloak_realm
      value: finmatica
    - name: keycloak_user_email
      value: user1
    - name: keycloak_user_password
      value: User123!
    - name: keycloak_admin_email
      value: admin
    - name: keycloak_admin_password
      value: Admin123!
    - name: keycloak_scope
      value: openid profile email roles
    - name: keycloak_client_id
      value: authtestproject-api
    - name: keycloak_client_secret
      value: authtestproject-api-secret
  scripts:
    - type: after-response
      code: |-
        const { printAccessToken } = require('./util.js');

        const body = res.getBody();
        //printAccessToken('Access Token',body.access_token);
