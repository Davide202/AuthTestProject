info:
  name: Api restricted to user role WSO2_MANAGER
  type: http
  seq: 1
  tags:
    - 4_Wso2_Manager_APIs

http:
  method: GET
  url: "{{baseUrl}}/api/reports/secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/reports/secret"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""

docs: Empty description
