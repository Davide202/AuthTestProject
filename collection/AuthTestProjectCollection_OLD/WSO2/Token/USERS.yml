info:
  name: USERS
  type: http
  seq: 7

http:
  method: GET
  url: https://localhost:9443/scim2/Users?filter=userName+Eq+testuser
  headers:
    - name: Accept
      value: application/json
  params:
    - name: filter
      value: userName+Eq+testuser
      type: query
  auth:
    type: bearer
    token: "{{wso2_jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
