info:
  name: SECRET 2
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/reports/secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
