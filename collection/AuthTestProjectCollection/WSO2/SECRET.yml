info:
  name: SECRET
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/auth/secret"
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
