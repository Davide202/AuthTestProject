info:
  name: CLAIMS
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/reports/debug"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
